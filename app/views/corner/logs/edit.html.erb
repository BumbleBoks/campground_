<% provide(:title, "Campground - #{current_user.name}'s Diary") %>
<h2>Change Log</h2>

<div class='project_form'>
	<%= form_for @log, url: { action: :update } do |f| -%>

		<%= render 'fields', { f: f } %>	
	  
	<% end -%>
	
</div>
